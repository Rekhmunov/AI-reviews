# ТЗ: Восстановление КИЗ/ШК при приёмке возвратов на склад

**Статус:** к реализации  
**Контур:** Остатки → Добавить на склад → тип «Возврат» + поверх — «Восстановить данные»  
**Связанные модули:** `supply-balances` (приход), `wb_fbs_returns` (журнал/печать возвратов WB)

---

## 1. Цель

Дать оператору склада **одно рабочее место** приёмки возвратов:

1. **Happy path** — как сейчас: скан ШК или КИЗ → товар в список → «Сохранить» → остатки.
2. **Exception path** — потерян/повреждён КИЗ или ШК: кнопка **«Восстановить данные»** открывает поверх (не закрывая приёмку) инструмент поиска по стикеру возврата / КИЗ с печатью маркировки и ШК; после печати оператор продолжает приёмку в той же модалке «Добавить на склад».

Не заставлять ходить в «ВБ ФБС → Возвраты» для ежедневной работы.

---

## 2. Роли и сценарии

### 2.1. Оператор склада (основной пользователь)

| # | Сценарий | Ожидание |
|---|----------|----------|
| A | На товаре есть читаемый ШК или КИЗ | Скан в «Добавить на склад» (Возврат) → товар +1 → Сохранить |
| B | Нет КИЗ / КИЗ не читается, есть стикер возврата WB | «Восстановить данные» → скан стикера возврата → печать КИЗ (и/или ШК) → закрыть → скан в приёмку |
| C | Нет ШК на товаре, нужно наклеить заново | Restore → найти возврат → «Печать ШК» → закрыть → скан ШК в приёмку |
| D | КИЗ есть, но этикетка порвана; нужно перепечатать | Restore → скан КИЗ → «Печать маркировки» → закрыть → приёмка |
| E | Стикер возврата не найден в кэше | Понятное сообщение; не блокировать приёмку по ШК/КИЗ, если они есть |

### 2.2. Админ / старший

Полный журнал «ВБ ФБС → Возвраты» (CSV, фильтры, ручной sync по источнику) **остаётся** без изменения поведения для админских задач. Складской restore — упрощённый операторский слой поверх той же серверной логики.

---

## 3. Текущее состояние (as-is)

### 3.1. Остатки → Добавить на склад

- UI: `#supplyStockReceiptModal`, тип `#supplyStockReceiptKind` = `receipt` | `return`.
- Скан: `#supplyStockReceiptScan` → `processSupplyStockReceiptScan()` — матч по каталогу товаров (ШК / GTIN из КИЗ).
- Сохранение: `POST /api/supply-balances/receipt`.
- **Ограничения:** стикер возврата WB не распознаётся; печати КИЗ/ШК нет; тип «Возврат» на бэкенде сейчас по сути тот же `manual_receipt` (косметика в UI).

### 3.2. ВБ ФБС → Возвраты

- UI: `#wbFbsKizRestoreModal` (`openWbFbsKizRestoreModal`).
- Sync: `POST /api/wb-fbs/returns/sync` + poll status — **один** `source_id`.
- Скан: `POST /api/wb-fbs/returns/scan` — стикер возврата / стикер сборки / КИЗ.
- Печать: `GET /api/wb-fbs/returns/print` (DataMatrix), ШК из `catalog_barcodes`.
- **Ограничения:** не пишет в остатки; sync по одному источнику; агрессивный multi-window sync бьёт в Analytics 429 на «тяжёлых» кабинетах.

---

## 4. Продуктовое решение (to-be)

```
┌─────────────────────────────────────────────┐
│  Добавить на склад  [тип: Возврат ▾]        │
│  [Скан ШК/КИЗ…………]                         │
│  [ Восстановить данные ]  ← только Return   │
│  список позиций…          [Сохранить]       │
└─────────────────────────────────────────────┘
              │ открыть поверх (z-index выше)
              ▼
┌─────────────────────────────────────────────┐
│  Восстановить данные (возврат WB)     [✕]   │
│  источники: фоновая очередь sync (не блок)  │
│  [Скан стикера возврата / КИЗ…………]          │
│  карточка результата + Печать КИЗ / ШК      │
└─────────────────────────────────────────────┘
```

После закрытия restore модалка приёмки **остаётся открытой**, фокус возвращается в `#supplyStockReceiptScan`.

---

## 5. UI / UX

### 5.1. Кнопка «Восстановить данные»

- Место: модалка `#supplyStockReceiptModal`, блок сканирования (рядом с `#supplyStockReceiptScan` или под ним).
- Видимость: **только** если `#supplyStockReceiptKind === "return"`.
- При переключении на «Приход» — кнопка скрыта; если restore открыт — закрыть restore.
- Стиль: secondary / outline, не конкурирует с primary «Сохранить».
- id (предложение): `#supplyStockReceiptRestoreBtn`.

### 5.2. Модалка «Восстановить данные»

Новая модалка (не переиспользовать целиком тяжёлый админский `#wbFbsKizRestoreModal` с таблицей/CSV/календарём), но **те же API и печать**.

Предлагаемый id: `#supplyStockReturnRestoreModal`.

**Состав первого экрана:**

1. Заголовок: «Восстановить данные».
2. Подзаголовок (1 строка): «Сканируйте стикер возврата WB или КИЗ для печати».
3. Компактный статус sync (не блокирует скан), например:  
   `Синхронизация возвратов: 2/4 · ошибка на «Кабинет X» (лимит WB)`.
4. Поле скана (Enter) — аналог `#wbFbsKizRestoreScan`.
5. Область результата (пусто / карточка / ошибка).
6. Кнопка ✕ — закрыть только restore.

**Карточка результата после успешного скана:**

- Заказ / стикер возврата / тип скана (Возврат | Маркировка | Стикер сборки).
- Товар: фото (если есть), название, артикул.
- КИЗ: полный код или «не найден».
- Кнопки:
  - **Печать маркировки** — enabled, если есть КИЗ; иначе disabled + подсказка.
  - **Печать ШК** — enabled, если есть barcodes в каталоге товаров; иначе disabled + подсказка.
- При нескольких ШК — тот же выбор, что в ВБ ФБС (`#wbFbsReturnsBarcodeModal` или аналог поверх restore).

**Не включать в v1 складского restore:** таблицу журнала, CSV, календарь, бесконечный скролл, фильтры типов (опционально позже).

### 5.3. Слои модалок (важно)

- `#supplyStockReceiptModal` остаётся открытой (`hidden` не ставить).
- `#supplyStockReturnRestoreModal` — поверх (выше z-index), `aria-modal="true"`.
- Закрытие restore **не** вызывает `closeSupplyStockReceiptModal`.
- Клик по оверлею restore: либо не закрывать (как у ВБ ФБС Возвраты), либо закрывать только restore — зафиксировать в реализации: **не закрывать по клику на оверлей** (меньше случайных закрытий со сканера).
- Esc: закрывает только верхнюю (restore), если она открыта.

### 5.4. После печати

- Печать — как сейчас (окно/iframe DataMatrix или JsBarcode 58×40).
- Restore **не** автозакрывается после печати (оператор может напечатать и КИЗ, и ШК).
- Подсказка в info: «Распечатайте этикетку, затем отсканируйте её в «Добавить на склад»».

---

## 6. Поведение скана в restore

Классификация — **та же**, что `process_return_scan` / `POST /api/wb-fbs/returns/scan`:

1. Похоже на КИЗ → `kiz` (перепечатка).
2. Есть в кэше `wb_fbs_goods_returns` → `return_sticker`.
3. Стикер сборки в `wb_fbs_orders` → `assembly_sticker`.
4. Иначе — not_found.

### 6.1. Отличие от админской модалки: все источники

Скан и поиск выполняются **по всем WB FBS-источникам** владельца (куда у пользователя есть доступ), а не по одному выбранному в селекте ВБ ФБС.

Варианты реализации (выбрать один в коде, предпочтение A):

| | Подход | Плюсы | Минусы |
|---|--------|-------|--------|
| **A** | Новый API: `POST /api/wb-fbs/returns/scan-all` — сервер итерирует источники, первый уверенный match | Один round-trip, проще UI | Нужна аккуратная дедупликация ambiguous |
| B | Клиент последовательно дергает `/scan` по списку source_id | Меньше бэкенда | Медленнее, сложнее UX |

**Правила match при scan-all:**

- Первый однозначный hit → вернуть результат + `source_id` + имя источника.
- Если несколько разных заказов/товаров по разным источникам → `ambiguous` (404/409 с текстом «Найдено в нескольких кабинетах — уточните стикер») без записи в журнал **или** запись не делать до подтверждения. Для v1: **не писать**, показать ошибку ambiguous.
- Duplicate стикера возврата в рамках одного source — как сейчас (409 already scanned), карточку всё равно показать для печати (важно для оператора!). Уточнение: при 409 админка показывает duplicate; для склада нужно **всё равно отдать данные для печати** (новый флаг ответа или отдельный lookup). См. §7.3.

Запись в `wb_fbs_return_scans` при складском restore:

- **Да**, писать при успешном новом скане (единый журнал с ВБ ФБС) — удобно для аудита.
- При duplicate — не создавать вторую строку, но вернуть payload для печати.

---

## 7. API

### 7.1. Существующие (переиспользовать)

| Метод | Путь | Назначение |
|-------|------|------------|
| POST | `/api/wb-fbs/returns/sync` | sync одного source |
| GET | `/api/wb-fbs/returns/sync/status` | статус job |
| POST | `/api/wb-fbs/returns/scan` | скан одного source |
| GET | `/api/wb-fbs/returns/print` | PNG DataMatrix |
| GET | `/api/supply-balances/visibility` | каталог + barcodes (уже для приёмки) |

### 7.2. Новые / расширения

#### 7.2.1. Список источников для restore

`GET /api/wb-fbs/returns/restore/sources`

Ответ:

```json
{
  "items": [
    {
      "source_id": 21,
      "name": "WB Кабинет …",
      "sync_status": "idle|running|ok|error",
      "sync_error": "",
      "last_synced_at": "…|null",
      "rate_limited": false,
      "rate_limit_until": null
    }
  ]
}
```

Доступ: как у WB FBS view.

#### 7.2.2. Запуск фоновой очереди sync всех источников

`POST /api/wb-fbs/returns/restore/sync-all`

- Не блокирует HTTP дольше старта job.
- Сервер ставит **очередь**: источники по одному (или по уникальному Analytics-токену — см. §8).
- Идемпотентность: если очередь уже `running` — вернуть `already_running`.
- Не стартовать параллельно N sync на разные кабинеты с одним и тем же Analytics uid.

Ответ:

```json
{
  "ok": true,
  "async": true,
  "status": "running",
  "job_id": "…",
  "source_ids": [19, 21],
  "message": "Фоновая синхронизация возвратов запущена"
}
```

#### 7.2.3. Статус очереди sync-all

`GET /api/wb-fbs/returns/restore/sync-all/status`

```json
{
  "status": "idle|running|ok|error",
  "sources": [ /* как в §7.2.1, актуальные */ ],
  "done": 2,
  "total": 4,
  "current_source_id": 21,
  "finished_at": null
}
```

UI поллит каждые 2–3 с, пока `running`, **не** блокируя поле скана.

#### 7.2.4. Скан по всем источникам (+ печать при duplicate)

`POST /api/wb-fbs/returns/restore/scan`

Body:

```json
{ "scan": "…" }
```

Ответ при успехе — совместим по полям с текущим `/returns/scan` result item + `source_id`, `source_name`.

Дополнительно:

```json
{
  "ok": true,
  "duplicate": false,
  "item": { /* карточка */ },
  "source_id": 21,
  "source_name": "…"
}
```

При duplicate:

```json
{
  "ok": true,
  "duplicate": true,
  "item": { /* те же поля для печати */ },
  "message": "Уже был в журнале — можно распечатать снова"
}
```

При not_found / ambiguous — 404 с понятным `detail`.

Печать: существующий `GET /returns/print?source_id=&order_id=&kiz=` (или как сейчас в UI).

---

## 8. Стратегия sync «все источники» (критично)

### 8.1. Запрещено в v1

- Параллельный `sync` всех источников сразу.
- Игнорировать 429 / ретраить в цикле без Retry-After.
- Блокировать UI скана до конца полной 90-дневной выгрузки.

### 8.2. Обязательно

1. **Очередь по Analytics-токену (uid JWT):** источники с одним `uid` — строго последовательно; разные uid можно слегка перекрывать (max 1–2 параллельных job), но безопаснее v1 = **строго один sync глобально в очереди restore**.
2. Между запросами goods-return — существующий cooldown (~61 с) + уважение к 429 / `X-RateLimit-Retry`.
3. При 429 на источнике: пометить источник `rate_limited`, **продолжить очередь** со следующим; не ронять весь job.
4. Период sync по умолчанию для restore-очереди: **не 90 дней**, а **короткое окно** (рекомендация: **14 или 31 день**), чтобы чаще укладываться в **1 HTTP-окно** и меньше ловить 429. Полные 90 дней — только в админской модалке ВБ ФБС.
5. Кэш `wb_fbs_goods_returns` использовать сразу при открытии: скан работает по уже загруженным данным, даже если sync ещё идёт.
6. Автозапуск sync-all при открытии restore — **да**, но тихий; повторный автозапуск не чаще 1 раза в N минут на пользователя (например 30–60 мин), плюс ручная кнопка «Обновить из WB» (опционально в v1.1).

### 8.3. Почему короткий период для склада

Ежедневная/через день приёмка возвратов не требует 90 дней истории. Короткое окно снижает число окон Analytics API и риск 429 (известная проблема кабинета с `uid:39682584`).

---

## 9. Связь с приёмкой на склад

### 9.1. Что не менять в v1

- Логику `processSupplyStockReceiptScan` (ШК / GTIN → qty++).
- `POST /api/supply-balances/receipt` контракт (можно позже добавить `kind=return` — вне обязательного scope v1).

### 9.2. Операторский цикл после restore

1. Печать КИЗ или ШК в restore.
2. Закрыть restore.
3. Отсканировать **распечатанный** ШК или КИЗ в `#supplyStockReceiptScan`.
4. Товар появляется в списке → «Сохранить».

Автоматически добавлять товар в список приёмки из карточки restore в **v1 не требуется** (меньше риска неверного qty / неверный товар). Опционально v1.1: кнопка «Добавить в приёмку» на карточке.

---

## 10. Нефункциональные требования

- Не ломать скорость скана в приёмке (restore — отдельный поток).
- Не вызывать ощущение 504: HTTP sync-all / scan-all ≤ нескольких секунд; долгая работа — только background job + poll.
- Мобильная ширина ≥375px: restore читаем, кнопки печати крупными.
- Доступ: те же права, что просмотр WB FBS / остатков (согласовать с `_can_view_wb_fbs` и доступом к supply balances).
- Логирование: start/finish sync-all, 429 per source, scan hit/miss/ambiguous (без сырых секретов ключей).

---

## 11. Вне scope (не делать в этой задаче)

- Ozon возвраты.
- Автоматическое списание/перемещение кроме ручного «Сохранить» приёмки.
- Замена админской модалки ВБ ФБС.
- Обязательный КИЗ для немаркируемых товаров.
- Исправление hydrate `IncorrectParameter` / лечение 429 кабинета 19 — отдельные задачи (но restore должен **устойчиво** переживать 429 на части источников).

---

## 12. Критерии приёмки

1. При типе «Приход» кнопки «Восстановить данные» нет.
2. При типе «Возврат» кнопка есть; открывает restore **поверх** приёмки; приёмка не закрывается.
3. Закрытие restore возвращает фокус в скан приёмки; список позиций приёмки не сбрасывается.
4. Скан стикера возврата (при наличии в кэше любого источника) показывает карточку с печатью КИЗ/ШК (если данные есть).
5. Скан КИЗ позволяет перепечатать маркировку.
6. При открытии restore запускается (или продолжается) фоновая sync-очередь; скан доступен сразу.
7. 429/ошибка на одном источнике не блокирует скан по данным других и не валит всю очередь.
8. Happy path приёмки по ШК/КИЗ без входа в restore работает как раньше.
9. Админская «ВБ ФБС → Возвраты» не регрессирует.

---

## 13. План реализации (этапы)

### Этап 1 — UI каркас
- Кнопка в receipt modal (только return).
- Модалка restore поверх, без закрытия parent.
- Статус-бар sync (заглушка / idle).

### Этап 2 — Backend sync-all + sources
- Очередь, status API, короткий период по умолчанию, изоляция 429.

### Этап 3 — Backend scan across sources
- `restore/scan` + корректный duplicate→всё равно печать.

### Этап 4 — Печать
- Подключить существующие print KIZ / barcode picker.

### Этап 5 — Тесты и полировка
- Юнит/контрактные тесты API; UI-smoke assertions в существующих test-файлах стиля page/js.
- Ручной чеклист: return + restore поверх; скан; печать; возврат в приёмку.

---

## 14. Ключевые файлы

| Файл | Изменения |
|------|-----------|
| `web_templates/app.html` | Кнопка + разметка `#supplyStockReturnRestoreModal` |
| `web_static/app.js` | open/close restore, scan, print, poll sync-all; toggle кнопки по kind |
| `web_static/style.css` | z-index слоёв, компактная карточка |
| `review_processor/web.py` | endpoints restore/* |
| `review_processor/wb_fbs_returns.py` | очередь sync-all, scan-all, duplicate printable payload |
| `tests/test_wb_fbs_returns.py` (+ новые) | очередь, scan-all, 429 isolation |
| `tests/` UI assertions | кнопка только для return, parent не закрывается |

---

## 15. Открытые решения (зафиксировать при старте кода)

1. Период sync-all по умолчанию: **14** или **31** день? (рекомендация: **31** — одно окно WB).
2. Писать ли каждый складской скан в `wb_fbs_return_scans`? (рекомендация: **да**, duplicate — printable без новой строки).
3. Нужна ли в v1 кнопка «Обновить из WB» вручную рядом со статусом? (рекомендация: **да**, простая).
4. Автодобавление в список приёмки из карточки? (рекомендация: **нет в v1**).

---

## 16. Резюме одной фразой

В «Добавить на склад → Возврат» остаётся быстрый скан ШК/КИЗ; кнопка «Восстановить данные» открывает поверх упрощённый поиск/печать по стикеру возврата и КИЗ сразу по всем кабинетам, с **фоновой** аккуратной sync-очередью и без закрытия приёмки.
